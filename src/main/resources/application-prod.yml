server:
  port: ${PORT:8080}

spring:
  application:
    name: vocab-app

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://ep-nameless-breeze-a1rzsdfn-pooler.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require}
    username: ${DATABASE_USERNAME:neondb_owner}
    password: ${DATABASE_PASSWORD:npg_fIo7cLnOug1s}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  web:
    cors:
      allowed-origins:
        - ${FRONTEND_URL:http://localhost:5173}
        - http://localhost:3000
        - http://localhost:5173
        - https://vocab-frontend-xi.vercel.app
        - https://vocab-frontend-delta.vercel.app
      allowed-methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      allowed-headers: "*"
      allow-credentials: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:your-secret-key-change-this-in-production-make-it-very-long-and-secure}
  expiration: 86400000

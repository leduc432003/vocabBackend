server:
  port: ${PORT:8080}

spring:
  application:
    name: vocab-app

  datasource:
    url: ${DATABASE_URL:jdbc:mysql://shinkansen.proxy.rlwy.net:36474/railway}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:cgwfWmZOoitduGacWrSEmlkPSMUeVcEW}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  web:
    cors:
      allowed-origins:
        - ${FRONTEND_URL:http://localhost:5173}
        - http://localhost:3000
        - http://localhost:5173
        - https://vocab-frontend-xi.vercel.app
        - https://vocab-frontend-delta.vercel.app
      allowed-methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      allowed-headers: "*"
      allow-credentials: true

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:your-secret-key-change-this-in-production-make-it-very-long-and-secure}
  expiration: 86400000
